<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /s/

    # Pass /s/{slug} to Laravel bootstrap (not a static file)
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L]
</IfModule>

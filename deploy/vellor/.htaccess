# Staging: samarthinfoservices.com/vellor/
# Marketing → dist/ · Laravel → admin/ · Storefront → /s/{slug}
<IfModule mod_rewrite.c>
    RewriteEngine On

    # 301: strip /public/ from admin URLs (skip index.php — avoids LiteSpeed 500 on /admin/index.php)
    RewriteCond %{ENV:REDIRECT_STATUS} ^$
    RewriteCond %{THE_REQUEST} \s[^\s]*/admin/public/([^\s?]*) [NC]
    RewriteCond %1 !^index\.php$ [NC]
    RewriteRule ^admin/public/(.*)$ admin/$1 [R=301,L,NE]

    RewriteRule ^admin/?$ admin/public/index.php [L]
    RewriteRule ^admin/(.*)$ admin/public/$1 [L]

    RewriteRule ^s(/.*)?$ admin/public/index.php [L]

    RewriteCond %{DOCUMENT_ROOT}/vellor/dist/$1 -f
    RewriteRule ^(.+)$ dist/$1 [L]

    RewriteCond %{DOCUMENT_ROOT}/vellor/dist/$1 -d
    RewriteRule ^(.+)$ dist/$1 [L]

    RewriteCond %{REQUEST_URI} !/admin(/|$) [NC]
    RewriteCond %{REQUEST_URI} !/s(/|$) [NC]
    RewriteRule ^ dist/index.html [L]
</IfModule>

DirectoryIndex dist/index.html
